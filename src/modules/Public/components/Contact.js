import { Box, Typography } from "@mui/material";

export default function Contact(){
    return(
        <Box sx={{ minHeight: "100vh" }} id="contact">
            <Typography>This is the Contact Section</Typography>
        </Box>
    )
}