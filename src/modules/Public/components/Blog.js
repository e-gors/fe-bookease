import { Box, Typography } from "@mui/material";

export default function Blog() {
  return (
    <Box sx={{ minHeight: "100vh" }} id="blog">
      <Typography>This is the Blog Section</Typography>
    </Box>
  );
}
